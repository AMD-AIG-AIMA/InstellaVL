#!/bin/bash
mkdir -p playground/data/pretraining_data/
COMMON_DIR="playground/data/pretraining_data"

# Consists of modified conversation .json files for all below mentioned datasets
mkdir "${COMMON_DIR}/json_files_processed"
# Below folders will consists of Images.
mkdir "${COMMON_DIR}/LLaVA-Stage1.5"
mkdir "${COMMON_DIR}/ArT"
mkdir "${COMMON_DIR}/chart2text_pew"
mkdir "${COMMON_DIR}/chart2text_statista"
mkdir "${COMMON_DIR}/coco_text"
mkdir "${COMMON_DIR}/docvqa"
mkdir "${COMMON_DIR}/hiertext"
mkdir "${COMMON_DIR}/mlt"
mkdir "${COMMON_DIR}/mmc_instruction_arxiv"
mkdir "${COMMON_DIR}/mmc_instruction_non-arxiv"
mkdir "${COMMON_DIR}/M-Paper-analysis"
mkdir "${COMMON_DIR}/M-Paper-cap"
mkdir "${COMMON_DIR}/M-Paper-outline"
mkdir "${COMMON_DIR}/openvino"
mkdir "${COMMON_DIR}/Paper2Fig100k"
mkdir "${COMMON_DIR}/PlotQA"
mkdir "${COMMON_DIR}/Rico_ScreenQA"
mkdir "${COMMON_DIR}/RICO-Screen2Words"
mkdir "${COMMON_DIR}/SciGraphQA-295K-train"
mkdir "${COMMON_DIR}/st_vqa"
mkdir "${COMMON_DIR}/textocr"
mkdir "${COMMON_DIR}/Uber-Text"
mkdir "${COMMON_DIR}/UniChart"
mkdir "${COMMON_DIR}/DocStruct4M_struct_aware_parse"
mkdir "${COMMON_DIR}/DocStruct4M_multi_grained_text_localization"
mkdir "${COMMON_DIR}/pixmo_cap_part_1"
mkdir "${COMMON_DIR}/pixmo_cap_part_2"
mkdir "${COMMON_DIR}/pixmo_cap_part_3"
mkdir "${COMMON_DIR}/pixmo_cap_part_4"
mkdir "${COMMON_DIR}/pixmo_cap_part_5"
